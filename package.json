{
  "name": "request-bin",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx nodemon app.js",
    "resetdb": "npm run dropdb && npm run setupdb",
    "setupdb": "npm run createdb && npm run loadschema && npm run loadseeddata",
    "createdb": "createdb requestbin",
    "dropdb": "dropdb requestbin", 
    "loadschema": "psql -d requestbin < schema.sql",
    "loadseeddata": "psql -d requestbin < seedData.sql"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "pg": "^8.15.6"
  }
}
